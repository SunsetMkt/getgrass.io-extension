export enum Languages {
  ENGLISH = "English",
  FRENCH = "French",
}

export enum Status {
  CONNECTED = "CONNECTED",
  DISCONNECTED = "DISCONNECTED",
  DEAD = "DEAD",
  CONNECTING = "CONNECTING",
}

export enum Actions {
  PAUSE = "pause-grass",
  LANGUAGE = "language",
}
